/var/log/bunker/*.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    create 0640 bunker bunker
    sharedscripts
    postrotate
        systemctl kill -s HUP pi-monitor.service >/dev/null 2>&1 || true
    endscript
}
